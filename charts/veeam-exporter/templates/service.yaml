apiVersion: v1
kind: Service
metadata:
  name: {{ include "veeam-exporter.fullname" . }}
  labels:
    {{- include "veeam-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "veeam-exporter.selectorLabels" . | nindent 4 }}
